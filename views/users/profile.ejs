<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Your Profile</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap + FontAwesome -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <style>
        body {
            background: linear-gradient(to right, #f5f7fa, #c3cfe2);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .profile-container {
            max-width: 750px;
            margin: 60px auto;
            padding: 30px;
        }

        .profile-card {
            background-color: #fff;
            border-radius: 20px;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
            overflow: hidden;
        }

        .profile-header {
            background: #7b4a9e;
            padding: 30px;
            text-align: center;
            color: #fff;
        }

        .profile-header img {
            width: 120px;
            height: 120px;
            object-fit: cover;
            border-radius: 50%;
            border: 4px solid #fff;
            margin-top: -60px;
            background-color: #eee;
        }

        .profile-body {
            padding: 30px;
        }

        .profile-info p {
            margin-bottom: 15px;
            font-size: 1.1rem;
        }

        .profile-info i {
            color: #7b4a9e;
            margin-right: 8px;
        }

        .btn-custom {
            border-radius: 30px;
            padding: 10px 25px;
            font-weight: 500;
        }

        .btn-primary {
            background-color: #7b4a9e;
            border-color: #7b4a9e;
        }

        .btn-primary:hover {
            background-color: #6a3b89;
        }

        .btn-secondary:hover {
            background-color: #555;
        }
    </style>
</head>

<body>
    <%- include('../partials/header') %>

    <div class="container profile-container">
        <div class="card profile-card">
            <div class="profile-header">
                <h2 class="mb-1">Your Profile</h2>
                <p class="mb-0">Welcome back, <strong><%= user.name %></strong>!</p>
            </div>
            <div class="text-center mt-n5">
            </div>
            <div class="profile-body">
                <div class="profile-info">
                    <p><i class="fas fa-user"></i><strong>Username:</strong> <%= user.username %></p>
                    <p><i class="fas fa-id-card"></i><strong>Name:</strong> <%= user.name %></p>
                    <p><i class="fas fa-envelope"></i><strong>Email:</strong> <%= user.email || 'N/A' %></p>
                    <p><i class="fas fa-phone"></i><strong>Phone:</strong> <%= user.phoneNumber || 'N/A' %></p>
                    <p><i class="fas fa-map-marker-alt"></i><strong>Address:</strong> <%= user.address || 'N/A' %></p>
                </div>

                <div class="d-flex justify-content-between mt-4">
                    <a href="/" class="btn btn-secondary btn-custom">
                        <i class="fas fa-arrow-left"></i> Back
                    </a>
                    <a href="/users/profile/edit" class="btn btn-primary btn-custom">
                        <i class="fas fa-edit"></i> Edit Profile
                    </a>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
</body>

</html>
